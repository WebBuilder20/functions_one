name: Flutter Web
on:
  push:
    branches:
      - main
jobs:
  build:
    name: Build Web
    env:
      my_secret: $
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: subosito/flutter-action@v1
        with:
          channel: 'dev'
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release
        working-directory: ./functions_one
      - run: |
          cd functions/web
          git init
          git config --global user.email localhost.ads@gmail.com
          git config --global user.name WebBuilder20
          git status
          git remote add main git@github.com:WebBuilder20/functions_one.git
          git checkout -b gh-pages
          git add --all
          git commit -m "update"
          git push main gh-pages -f